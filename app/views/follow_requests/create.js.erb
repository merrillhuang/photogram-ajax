var follow_unfollow = $(".sender_<%= @follow_request.sender_id %>_recipient_<%= @follow_request.recipient_id %>");

follow_unfollow.replaceWith("<%= escape_javascript(render "follow_requests/follow_unfollow", sender: current_user, recipient: @follow_request.recipient) %>");
